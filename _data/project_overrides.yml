# Per-repo overrides for the auto-populated highlighted projects.
# Keyed by GitHub "owner/repo". Any field set here wins over the value pulled
# from the GitHub API. Common use is `thumbnail:` since GitHub has no
# equivalent.
#
# Set `personal: true` to mark a repo as non-work: it is hidden on the about
# page behind the "Show personal projects" toggle. (A `personal` GitHub topic
# does the same thing without needing an entry here.)

micah-olivas/beak:
  thumbnail: assets/img/beak_thumbnail.jpg

FordyceLab/usortm:
  name: uSort-M
  thumbnail: assets/img/publication_preview/usortm-icon.png

micah-olivas/array-print:
  name: ArrayPrint

micah-olivas/whisper-cleaner:
  personal: true
